{% extends "bases_admin.html.twig" %}

{% block main %}
    <main>
        <h3>{{ product.name }}</h3>
        <p>{{ product.description }}</p>
        <p>{{ product.price }}</p>
    </main>
{% endblock %}